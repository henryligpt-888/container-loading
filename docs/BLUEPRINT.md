# 3D 智能装箱模拟器 - 项目蓝图

## 📋 项目概述

### 项目名称
3D Container Packer (3D智能装箱模拟器)

### 项目描述
这是一个基于 React、Three.js 和 TypeScript 开发的 3D 货柜装箱可视化应用程序。该系统提供交互式的三维可视化界面，帮助用户模拟和优化货柜装载方案，支持多种集装箱规格和智能装箱算法。

### 核心价值
- **可视化决策支持**：通过 3D 渲染直观展示装箱方案
- **空间优化**：智能算法最大化货柜空间利用率
- **实时反馈**：即时计算并显示装载统计数据
- **多场景支持**：支持多种标准集装箱规格（20GP、40GP、40HQ等）

---

## 🏗️ 技术架构

### 技术栈
| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **前端框架** | React | 19.2.0 | UI 组件化开发 |
| **语言** | TypeScript | 5.8.2 | 类型安全 |
| **3D渲染** | Three.js | 0.181.2 | WebGL 3D渲染引擎 |
| **3D抽象层** | @react-three/fiber | 9.4.0 | React 集成 Three.js |
| **3D辅助库** | @react-three/drei | 10.7.7 | 预制 3D 组件 |
| **构建工具** | Vite | 6.2.0 | 快速开发构建 |
| **图标库** | lucide-react | 0.554.0 | UI 图标 |
| **唯一ID** | uuid | 13.0.0 | 生成货物唯一标识 |

### 项目结构
```
3d-container-packer/
├── components/              # React 组件
│   ├── BoxMesh.tsx         # 3D 货物网格组件
│   ├── ControlPanel.tsx    # 左侧控制面板
│   └── Scene.tsx           # 3D 场景主容器
├── services/               # 业务逻辑服务
│   └── packingAlgorithm.ts # 装箱算法实现
├── App.tsx                 # 应用主入口
├── types.ts                # TypeScript 类型定义
├── constants.ts            # 常量配置（集装箱规格等）
├── index.tsx               # React 渲染入口
├── index.html              # HTML 模板
├── vite.config.ts          # Vite 配置
├── tsconfig.json           # TypeScript 配置
└── package.json            # 项目依赖
```

---

## 🧩 核心模块

### 1. 数据模型 (types.ts)

#### BoxItem（货物项）
```typescript
interface BoxItem {
  id: string;              // 唯一标识
  width: number;           // 长度（cm）
  height: number;          // 高度（cm）
  depth: number;           // 宽度（cm）
  weight: number;          // 重量（kg）
  cantStackTop: boolean;   // 是否禁止上方堆叠（易碎品标记）
  color: string;           // 显示颜色
  position?: [x, y, z];    // 3D空间位置（左下前角坐标）
  placed: boolean;         // 是否已成功装入
}
```

#### ContainerSpec（集装箱规格）
```typescript
interface ContainerSpec {
  name: string;            // 型号名称（如 20GP）
  width: number;           // 内部宽度（cm）
  height: number;          // 内部高度（cm）
  depth: number;           // 内部深度（cm）
  maxLoad: number;         // 最大载重（kg）
  label: string;           // 显示标签
  description?: string;    // 描述信息
}
```

### 2. 装箱算法 (services/packingAlgorithm.ts)

#### 算法类型
**贪心式空间填充算法（Greedy Space Filling）**

#### 算法流程
1. **预处理排序**
   - 按高度降序排序（优先放置高度大的货物）
   - 次要排序：按体积降序

2. **动态放置点管理**
   - 维护候选放置点列表（初始为原点 [0,0,0]）
   - 每次放置新货物后生成 3 个新候选点：
     - 货物顶部 (x, y+h, z)
     - 货物右侧 (x+w, y, z)
     - 货物前方 (x, y, z+d)

3. **位置验证（isValidPosition）**
   - 边界检查：货物不超出集装箱范围
   - 碰撞检测：使用 AABB（轴对齐包围盒）算法
   - 约束检查：易碎品禁止上方堆叠规则

4. **碰撞检测（checkOverlap）**
   ```
   重叠条件（三轴同时满足）：
   - X轴: ax < bx + bw && ax + aw > bx
   - Y轴: ay < by + bh && ay + ah > by
   - Z轴: az < bz + bd && az + ad > bz
   ```

5. **易碎品约束**
   - 检测水平面（XZ平面）重叠
   - 若新货物 Y 坐标 ≥ 易碎品 Y 坐标，则禁止放置

#### 算法特点
- ✅ 时间复杂度：O(n² × m)（n=货物数，m=候选点数）
- ✅ 空间优化：优先底部、后方、左侧（Y→Z→X优先级）
- ✅ 实用性强：支持特殊约束（易碎品保护）
- ⚠️ 局限性：贪心算法不保证全局最优解

### 3. 3D 可视化系统 (components/)

#### Scene.tsx（3D 场景）
**关键功能：**
- 自适应相机控制：根据集装箱尺寸自动调整视角
- 光照系统：环境光 + 平行光 + 半球光
- 集装箱渲染：半透明墙体 + 边框线条 + 地板网格
- 动态阴影：ContactShadows 接触阴影

**坐标系统：**
```
集装箱本地坐标：
  - 原点 (0,0,0) 位于左下后角
  - X轴：向右（宽度）
  - Y轴：向上（高度）
  - Z轴：向前（深度）
```

#### BoxMesh.tsx（货物网格）
**功能特性：**
- 悬停交互：鼠标悬停显示货物详情
- 易碎品标记：顶部红色半透明平面
- 边缘线条：黑色描边增强立体感
- HTML 标签：使用 @react-three/drei 的 Html 组件

#### ControlPanel.tsx（控制面板）
**核心功能模块：**
1. **集装箱选择器**
   - 6 种标准集装箱（20GP/40GP/40HQ/45HQ/20RF/40RF）
   - 实时显示内尺寸和最大载重

2. **货物添加表单**
   - 尺寸输入（长/宽/高）
   - 重量输入
   - 批量添加（数量设置）
   - 12 种预设颜色选择
   - 易碎品标记复选框

3. **货物清单**
   - 动态列表展示
   - 状态指示（已装入/未装入）
   - 单个删除 + 批量清空

4. **操作按钮**
   - 开始装箱计算
   - 重置位置
   - 清空列表

### 4. 统计仪表盘 (App.tsx)

**实时统计指标：**
| 指标 | 计算方式 | 用途 |
|------|---------|------|
| 空间利用率 | (已用体积 / 总体积) × 100% | 优化装载方案 |
| 重量利用率 | (已载重 / 最大载重) × 100% | 避免超载 |
| 装载数量 | 已放置 / 总货物数 | 评估成功率 |
| 剩余载重 | 最大载重 - 已载重量 | 容量预警 |

---

## 🎨 用户界面设计

### 布局结构
```
┌─────────────────────────────────────────────────┐
│  [统计仪表盘]                                    │
├──────────┬──────────────────────────────────────┤
│          │                                      │
│  控制    │         3D 可视化区域                │
│  面板    │      （可旋转/缩放/平移）             │
│          │                                      │
│  - 集装箱│         [操作指南]                   │
│  - 添加货│                                      │
│  - 货物列│                                      │
│  - 操作按│                                      │
└──────────┴──────────────────────────────────────┘
```

### 颜色系统
- **主色调**：蓝色（#3b82f6）- 操作按钮、强调元素
- **成功状态**：绿色（#10b981）- 已装入货物
- **警告状态**：黄色（#f59e0b）- 易碎品标记
- **错误状态**：红色（#ef4444）- 未装入货物、超载警告
- **中性色**：灰色系（#64748b）- 背景、边框

### 响应式设计
- 固定宽度侧边栏（450px）
- 自适应 3D 视口
- 触控友好按钮尺寸（最小 44×44px）

---

## 🔧 配置与常量

### 集装箱规格数据 (constants.ts)
| 型号 | 内尺寸 (L×D×H cm) | 最大载重 (kg) | 说明 |
|------|------------------|---------------|------|
| 20GP | 589.8×235.2×239.3 | 28,000 | 20尺普柜 |
| 40GP | 1203.2×235.2×239.3 | 26,000 | 40尺普柜 |
| 40HQ | 1203.2×235.2×269.8 | 26,000 | 40尺高柜 |
| 45HQ | 1355.6×235.2×269.8 | 25,000 | 45尺高柜 |
| 20RF | 545.0×226.0×225.0 | 27,000 | 20尺冷冻柜 |
| 40RF | 1155.0×228.0×225.0 | 29,000 | 40尺冷冻柜 |

### 预设颜色库
12 种鲜明色彩，便于区分不同货物类型：
```javascript
["#ef4444", "#f97316", "#f59e0b", "#84cc16",
 "#10b981", "#06b6d4", "#3b82f6", "#8b5cf6",
 "#d946ef", "#f43f5e", "#64748b", "#78350f"]
```

---

## 🚀 开发与部署

### 本地开发
```bash
# 安装依赖
npm install

# 启动开发服务器（端口 5173）
npm run dev

# 生产构建
npm run build

# 预览生产构建
npm run preview
```

### 环境要求
- Node.js: >= 18.0.0
- 浏览器: 支持 WebGL 2.0（Chrome 56+, Firefox 51+, Safari 15+）

### 性能优化建议
1. **Three.js 优化**
   - 使用 `dpr={[1, 2]}` 限制设备像素比
   - 阴影贴图分辨率 2048×2048
   - 控件阻尼系数 0.05（平滑运动）

2. **React 优化**
   - `useMemo` 缓存统计计算
   - 组件懒加载（可选）
   - 虚拟化货物列表（大量货物时）

---

## 📊 数据流架构

```
用户操作
   ↓
ControlPanel (添加货物/选择集装箱)
   ↓
App.tsx (状态管理)
   ├─→ calculatePacking (算法计算)
   │      ↓
   │   更新 BoxItem.position 和 placed 状态
   │      ↓
   ├─→ useMemo (统计计算)
   │      ↓
   └─→ Scene (3D渲染)
          ├─→ BoxMesh (逐个渲染货物)
          └─→ Container (渲染集装箱框架)
```

---

## 🐛 已知限制与改进方向

### 当前限制
1. **算法局限性**
   - 贪心算法不保证全局最优
   - 不支持货物旋转（固定方向）
   - 不考虑重心平衡

2. **功能缺失**
   - 无撤销/重做功能
   - 不支持保存/加载方案
   - 缺少装箱报告导出

3. **性能瓶颈**
   - 大量货物（>500）时可能卡顿
   - 候选点数量未做优化裁剪

### 改进建议
#### 短期优化
- [ ] 实现货物旋转功能（6种方向）
- [ ] 添加撤销/重做栈
- [ ] 方案导出（JSON/PDF报告）
- [ ] 货物预设模板（常见尺寸）

#### 中期增强
- [ ] 多货柜优化（自动分配多个集装箱）
- [ ] 遗传算法/模拟退火算法选项
- [ ] 重心计算与可视化
- [ ] 装载顺序动画演示

#### 长期规划
- [ ] 后端 API 集成（保存方案到云端）
- [ ] 多用户协作功能
- [ ] AI 推荐装载方案
- [ ] VR/AR 查看模式

---

## 📝 代码规范

### TypeScript 使用原则
- 所有组件必须定义 Props 接口
- 避免使用 `any` 类型
- 优先使用 `interface` 而非 `type`（数据模型）
- 使用严格模式（strict: true）

### React 最佳实践
- 函数式组件 + Hooks
- Props 解构传递
- 事件处理函数以 `handle` 开头
- 组件文件名使用 PascalCase

### CSS 样式策略
- Tailwind CSS 内联类名
- 响应式设计优先
- 禁用状态样式统一（opacity-50, cursor-not-allowed）

---

## 🔐 安全性考虑

### 输入验证
- 数值范围检查（长宽高 > 0）
- 最大数量限制（1000件）
- 类型转换错误处理（parseFloat/parseInt）

### XSS 防护
- React 自动转义文本内容
- UUID 生成避免冲突

### 性能安全
- 算法超时保护（setTimeout 异步执行）
- 内存泄漏防护（useEffect 清理函数）

---

## 📖 参考资料

### 算法理论
- 3D Bin Packing Problem (3D-BPP)
- Guillotine Algorithm（断头台算法）
- First Fit Decreasing (FFD)

### 技术文档
- [Three.js 官方文档](https://threejs.org/docs/)
- [React Three Fiber](https://docs.pmnd.rs/react-three-fiber/)
- [Vite 配置指南](https://vitejs.dev/config/)

---

## 📞 维护信息

### 项目仓库
- GitHub: [henryligpt-888/container-loading](https://github.com/henryligpt-888/container-loading)

### 版本历史
- v0.0.0 (2025-01-21): 初始版本发布

---

**最后更新**: 2025-01-21
**文档版本**: 1.0.0
